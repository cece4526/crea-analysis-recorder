<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rapport de Production SOJA - OF {{ of.numero }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: 12px;
            margin: 0;
            padding: 20px;
            color: #333;
        }
        
        .header {
            text-align: center;
            margin-bottom: 30px;
            border-bottom: 2px solid #2c5d31;
            padding-bottom: 20px;
        }
        
        .header h1 {
            color: #2c5d31;
            font-size: 24px;
            margin: 0;
        }
        
        .header .info {
            margin-top: 10px;
            font-size: 14px;
        }
        
        .section {
            margin-bottom: 25px;
            page-break-inside: avoid;
        }
        
        .section h2 {
            color: #2c5d31;
            font-size: 16px;
            border-bottom: 1px solid #2c5d31;
            padding-bottom: 5px;
            margin-bottom: 15px;
        }
        
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 15px;
            font-size: 11px;
        }
        
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }
        
        th {
            background-color: #f8f9fa;
            font-weight: bold;
            color: #2c5d31;
        }
        
        tr:nth-child(even) {
            background-color: #f9f9f9;
        }
        
        .conformite-oui {
            color: #28a745;
            font-weight: bold;
        }
        
        .conformite-non {
            color: #dc3545;
            font-weight: bold;
        }
        
        .summary-box {
            background-color: #e9f7ef;
            border: 1px solid #2c5d31;
            padding: 15px;
            border-radius: 5px;
            margin-bottom: 20px;
        }
        
        .summary-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 5px;
        }
        
        .footer {
            position: fixed;
            bottom: 0;
            left: 0;
            right: 0;
            height: 30px;
            text-align: center;
            font-size: 10px;
            color: #666;
            border-top: 1px solid #ddd;
            padding-top: 5px;
        }
        
        .text-center { text-align: center; }
        .text-right { text-align: right; }
        .font-bold { font-weight: bold; }
    </style>
</head>
<body>
    <div class="header">
        <h1>Rapport de Production SOJA</h1>
        <div class="info">
            <strong>OF N° {{ of.numero }}</strong> - {{ of.nature }}<br>
            Production du {{ production.date|date('d/m/Y à H:i') }}<br>
            Quantité totale: {{ production.quantiteTotale }} kg<br>
            Généré le {{ generated_at|date('d/m/Y à H:i:s') }}
        </div>
    </div>

    <!-- Résumé Global -->
    <div class="section">
        <h2>📊 Résumé de Production</h2>
        <div class="summary-box">
            <div class="summary-row">
                <span><strong>Nombre total d'analyses:</strong></span>
                <span>{{ data.summary.total_analyses }}</span>
            </div>
            <div class="summary-row">
                <span><strong>Durée de production:</strong></span>
                <span>{{ data.summary.duree_production ?? 'N/A' }}</span>
            </div>
            <div class="summary-row">
                <span><strong>pH moyen:</strong></span>
                <span>{{ data.summary.moyenne_ph ?? 'N/A' }}</span>
            </div>
            <div class="summary-row">
                <span><strong>Brix moyen:</strong></span>
                <span>{{ data.summary.moyenne_brix ?? 'N/A' }}°</span>
            </div>
            <div class="summary-row">
                <span><strong>Corrections avant:</strong></span>
                <span>{{ data.summary.total_corrections_av }}</span>
            </div>
            <div class="summary-row">
                <span><strong>Corrections après:</strong></span>
                <span>{{ data.summary.total_corrections_ap }}</span>
            </div>
            <div class="summary-row">
                <span><strong>Quantité Okara totale:</strong></span>
                <span>{{ data.summary.quantite_okara_totale }} kg</span>
            </div>
            <div class="summary-row">
                <span><strong>Conformité globale:</strong></span>
                <span class="{{ data.summary.conformite_globale ? 'conformite-oui' : 'conformite-non' }}">
                    {{ data.summary.conformite_globale ? 'CONFORME' : 'NON CONFORME' }}
                </span>
            </div>
        </div>
    </div>

    <!-- HACCP -->
    {% if data.haccp %}
    <div class="section">
        <h2>🛡️ Contrôles HACCP</h2>
        <table>
            <thead>
                <tr>
                    <th>Heure</th>
                    <th>Point de contrôle</th>
                    <th>Température</th>
                    <th>Durée</th>
                    <th>Conformité</th>
                    <th>Observations</th>
                </tr>
            </thead>
            <tbody>
                {% for haccp in data.haccp %}
                <tr>
                    <td>{{ haccp.heureControle|date('H:i') }}</td>
                    <td>{{ haccp.pointControle }}</td>
                    <td class="text-center">{{ haccp.temperature }}°C</td>
                    <td class="text-center">{{ haccp.duree }} min</td>
                    <td class="text-center {{ haccp.conformite ? 'conformite-oui' : 'conformite-non' }}">
                        {{ haccp.conformite ? 'OUI' : 'NON' }}
                    </td>
                    <td>{{ haccp.observations ?? '-' }}</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
    {% endif %}

    <!-- Analyses SOJA -->
    {% if data.analyses %}
    <div class="section">
        <h2>🔬 Analyses SOJA (toutes les 1h30 max)</h2>
        <table>
            <thead>
                <tr>
                    <th>Heure</th>
                    <th>pH</th>
                    <th>Brix</th>
                    <th>Température</th>
                    <th>Densité</th>
                    <th>Couleur</th>
                    <th>Goût</th>
                    <th>Conformité</th>
                    <th>Observations</th>
                </tr>
            </thead>
            <tbody>
                {% for analyse in data.analyses %}
                <tr>
                    <td class="font-bold">{{ analyse.heureAnalyse|date('H:i') }}</td>
                    <td class="text-center">{{ analyse.ph }}</td>
                    <td class="text-center">{{ analyse.brix }}°</td>
                    <td class="text-center">{{ analyse.temperature }}°C</td>
                    <td class="text-center">{{ analyse.densite }}</td>
                    <td>{{ analyse.couleur }}</td>
                    <td>{{ analyse.gout }}</td>
                    <td class="text-center {{ analyse.conformite ? 'conformite-oui' : 'conformite-non' }}">
                        {{ analyse.conformite ? 'OUI' : 'NON' }}
                    </td>
                    <td>{{ analyse.observations ?? '-' }}</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
    {% endif %}

    <!-- Corrections Avant -->
    {% if data.av_corrections %}
    <div class="section">
        <h2>⚙️ Corrections AVANT Production</h2>
        <table>
            <thead>
                <tr>
                    <th>Heure</th>
                    <th>Type Correction</th>
                    <th>Valeur Avant</th>
                    <th>Valeur Après</th>
                    <th>Quantité Ajoutée</th>
                    <th>Opérateur</th>
                    <th>Observations</th>
                </tr>
            </thead>
            <tbody>
                {% for correction in data.av_corrections %}
                <tr>
                    <td>{{ correction.heureCorrection|date('H:i') }}</td>
                    <td class="font-bold">{{ correction.typeCorrection }}</td>
                    <td class="text-center">{{ correction.valeurAvant }}</td>
                    <td class="text-center">{{ correction.valeurApres }}</td>
                    <td class="text-center">{{ correction.quantiteAjoutee }}</td>
                    <td>{{ correction.operateur }}</td>
                    <td>{{ correction.observations ?? '-' }}</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
    {% endif %}

    <!-- Corrections Après -->
    {% if data.ap_corrections %}
    <div class="section">
        <h2>🔧 Corrections APRÈS Production</h2>
        <table>
            <thead>
                <tr>
                    <th>Heure</th>
                    <th>Type Correction</th>
                    <th>Valeur Avant</th>
                    <th>Valeur Après</th>
                    <th>Quantité Ajoutée</th>
                    <th>Opérateur</th>
                    <th>Observations</th>
                </tr>
            </thead>
            <tbody>
                {% for correction in data.ap_corrections %}
                <tr>
                    <td>{{ correction.heureCorrection|date('H:i') }}</td>
                    <td class="font-bold">{{ correction.typeCorrection }}</td>
                    <td class="text-center">{{ correction.valeurAvant }}</td>
                    <td class="text-center">{{ correction.valeurApres }}</td>
                    <td class="text-center">{{ correction.quantiteAjoutee }}</td>
                    <td>{{ correction.operateur }}</td>
                    <td>{{ correction.observations ?? '-' }}</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
    {% endif %}

    <!-- Okara -->
    {% if data.okara %}
    <div class="section">
        <h2>🥜 Gestion Okara</h2>
        <table>
            <thead>
                <tr>
                    <th>Heure</th>
                    <th>Quantité (kg)</th>
                    <th>Qualité</th>
                    <th>Humidité (%)</th>
                    <th>Destination</th>
                    <th>Opérateur</th>
                    <th>Observations</th>
                </tr>
            </thead>
            <tbody>
                {% for okara in data.okara %}
                <tr>
                    <td>{{ okara.heureRecolte|date('H:i') }}</td>
                    <td class="text-center font-bold">{{ okara.quantite }} kg</td>
                    <td>{{ okara.qualite }}</td>
                    <td class="text-center">{{ okara.humidite }}%</td>
                    <td>{{ okara.destination }}</td>
                    <td>{{ okara.operateur }}</td>
                    <td>{{ okara.observations ?? '-' }}</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
    {% endif %}

    <div class="footer">
        CRÉA-ANALYSIS RECORDER - Rapport SOJA OF {{ of.numero }} - Page <span class="pageNumber"></span>
    </div>
</body>
</html>
